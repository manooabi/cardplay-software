<form method="post" action="{{ path('app_admin_software_version_delete', {'id': software_version.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ software_version.id) }}">
    <button class="btn">Delete</button>
</form>
